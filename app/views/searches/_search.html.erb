 <div class="mx-auto">
  <%= form_with url:search_path, local: true, method: :get do |f| %>
   <%= f.text_field :keyword %>
   <%= f.select :range, options_for_select([["User"],["Book"]]) %>
   <%= f.select :search, options_for_select([["完全一致"],["前方一致"],["後方一致"],["部分一致"]]) %>
   <%= f.submit '検索' %>
   <% end %>
</div>